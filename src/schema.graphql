type Query {
    info: String!
    feed: [Link!]!
    getLink(id: ID!): Link
  }

  type Mutation {
    post(url: String!, description: String!): Link!
  }

  type Link {
    id: String!
    description: String!
    url: String!
  }